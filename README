Installation:

    git clone git://github.com/brendonreed/dotvim.git ~/.vim
    on Windows:
    git clone git://github.com/brendonreed/dotvim.git C:\Program\ Files\ \(x86\)\Vim\vimfiles

Create symlinks or set pointer :

    ln -s ~/.vim/vimrc ~/.vimrc
    ln -s ~/.vim/gvimrc ~/.gvimrc

Or, in ~\_vimrc put:
    source C:\Program\ Files\ (x86)\Vim\vimfiles\vimrc

Switch to the `~/.vim` directory, and fetch submodules:

    cd ~/.vim
    git submodule init
    git submodule update
